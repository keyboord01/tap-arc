/** External links used on the landing page. */
export const GITHUB_URL = "https://github.com/keyboord01/tap-arc";

/** Profile on X. Leave empty to hide the link. */
export const X_URL = "";

/** Contract source, used for "Read the contract" when no deployed address is configured. */
export const CONTRACT_SOURCE_URL = `${GITHUB_URL}/blob/main/contracts/src/Tap.sol`;

/** Where every "Open Tap" button goes. */
export const APP_PATH = "/app";
